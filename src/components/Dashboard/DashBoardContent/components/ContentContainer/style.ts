import styled from "styled-components";

export const ContentContainerStyle = styled.ul`
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;
    padding: 2rem;
    background-color: var(--color-grey-3);
    border-radius: 4px;
    max-height: 500px;
    overflow-y: auto;
`